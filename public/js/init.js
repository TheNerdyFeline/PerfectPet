$(document).ready(function() {   
    $(".button-collapse").sideNav();
    $("#login-modal").modal();

    // waiting to find what this does
    $(".parallax").parallax();    
}); // end of document ready
